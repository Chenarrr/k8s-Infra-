apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: notes-app
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: notes-app
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: backend
  ports:
    - port: 5000
      targetPort: 5000
